// src/components/MainContent.jsx

// Define a functional component named MainContent.
// This component will hold the primary content of our page.
function MainContent() {
    return (
        // The <main> HTML tag is used for semantic meaning,
        // indicating this is the dominant content of the <body>.
        <main>
            {/* A simple paragraph with text about cities. */}
            <p>I love to visit New York, Paris, and Tokyo.</p>
        </main>
    );
}

// Export this component so it can be used elsewhere.
export default MainContent;